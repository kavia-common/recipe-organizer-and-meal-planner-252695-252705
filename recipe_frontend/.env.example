# Frontend environment variables
# Base URL for the backend API. Examples:
# - When running locally with backend at http://localhost:8000 and no proxy: http://localhost:8000
# - When serving via same origin and backend is reverse-proxied under /api: leave unset to use /api
REACT_APP_API_BASE_URL=/api
